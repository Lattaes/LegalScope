# Requirement run backend API:
- Install express:
  ```bash
  npm i express
  ```

- Install MongoDB:
 npm install mongodb

- Install Mongoose for accessing mongoDB from Node.js:
  npm install mongoose
